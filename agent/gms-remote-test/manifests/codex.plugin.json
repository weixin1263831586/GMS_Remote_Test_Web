{
  "name": "gms-remote-test",
  "version": "0.17.1",
  "description": "Operate the GMS Remote Test platform through the bundled gms-rt CLI: device inventory, CTS/GTS/VTS/STS execution, durable job status, reports, firmware burn, USB/IP, VPN. Self-contained; requires GMS_REMOTE_TEST_SERVER. Authenticates via GMS_AUTH_TOKEN_FILE (Agent Service Token, 0600 file) so no platform password ever flows through the model; adds read-only device shell (gms_rt_shell), typed logcat capture (gms_rt_logcat), one-shot approved device shell (gms_rt_shell_exec with server-side approval tokens), admin step-up elevation (gms_rt_auth_elevate) and typed firmware burn (gms_rt_burn_firmware, approval-token gated); jobs output is rendered one line per job.",
  "author": {
    "name": "GMS Remote Test Team"
  },
  "skills": "./skills/",
  "interface": {
    "displayName": "GMS Remote Test",
    "shortDescription": "Operate and diagnose the authenticated GMS test platform",
    "longDescription": "Discover devices across Workers, run CTS/GTS/VTS/STS jobs, follow durable results, inspect console/logcat evidence, and perform approval-gated maintenance through the bundled Agent Service Token boundary.",
    "developerName": "GMS Remote Test Team",
    "category": "Developer Tools",
    "capabilities": ["Read", "Write"],
    "defaultPrompt": [
      "Check GMS environment health and list available devices.",
      "Start a test and follow its durable job to completion.",
      "Inspect device console, snapshot, and logcat evidence."
    ],
    "brandColor": "#2563EB"
  },
  "mcpServers": {
    "gms": {
      "command": "./scripts/mcp_launcher.py",
      "args": [],
      "cwd": "./",
      "env": {
        "GMS_AGENT_CLIENT": "codex"
      }
    }
  }
}
